import styled from "styled-components";

const StyledFooter = styled.footer`

`

export const Footer: React.FC = () => {

    return (
        <StyledFooter>
            Developers
        </StyledFooter>
    )
}